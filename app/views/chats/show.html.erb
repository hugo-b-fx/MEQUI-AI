<<<<<<< HEAD
<div class="container py-5 min-vh-100 d-flex flex-column">
  <h1 class="text-center mb-5">MequiBot IA 🐎</h1>

  <div id="messages" class="flex-grow-1 overflow-auto mb-4 border rounded p-4 bg-white" data-controller="chat">
    <%= render @chat.messages %>
  </div>

  <%= simple_form_for [@chat, @message], url: chat_messages_path(@chat), data: { turbo_stream: true } do |f| %>
    <div class="input-group">
      <%= f.input :content, label: false, placeholder: "Pose ta question à MequiBot...", input_html: { class: "form-control rounded-start", data: { action: "keydown->chat#sendOnEnter" } } %>
      <%= f.submit "Envoyer", class: "btn btn-primary rounded-end" %>
    </div>
=======
<div class="container py-5">
  <h1 class="text-center mb-4">MequiBot IA 🐎</h1>

  <div id="messages" class="mb-4" style="height: 70vh; overflow-y: auto;">
    <%= render partial: "messages/message", collection: @messages %>
  </div>

  <%= simple_form_for [@chat, @message], url: chat_messages_path(@chat), data: { turbo: true } do |f| %>
    <%= f.input :content, label: false, placeholder: "Pose ta question ou dis-moi ce que tu cherches...", input_html: { class: "w-100" } %>
    <%= f.submit "Envoyer", class: "btn btn-primary" %>
>>>>>>> master
  <% end %>
</div>

<%= turbo_stream_from @chat %>
