<div class="chat-container">
  <h1 class="chat-title">MequiBot IA 🐎</h1>

  <div id="messages" data-controller="chat">
    <%= render @chat.messages %>
  </div>

  div>

  <%= simple_form_for [@chat, @message], url: chat_messages_path(@chat), data: { turbo_stream: true } do |f| %>
    <div class="input-group">
      <%= f.input :content, label: false, placeholder: "Posez votre question avec élégance...", input_html: { class: "form-control", data: { action: "keydown->chat#sendOnEnter" } } %>
      <%= f.submit "Envoyer", class: "btn btn-primary" %>
    </div>
  <% end %>
</div>

<%= turbo_stream_from @chat %>
